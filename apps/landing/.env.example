# Base de la API (incluye el prefijo global /api/v1)
VITE_API_URL=http://localhost:4001/api/v1

# Dominio público de la landing para canonical/og:url/sitemap.
# En Vercel se usa VERCEL_PROJECT_PRODUCTION_URL automáticamente si no está.
# VITE_SITE_URL=https://role.app
