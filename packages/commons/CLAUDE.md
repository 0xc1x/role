# Claude Code — punto de entrada

El contenido canónico vive en `AGENTS.md` de este directorio y en `AGENTS.md` de la raíz del monorepo. Este archivo es solo el punto de entrada para Claude Code — **no duplica contenido** para evitar drift entre copias.
